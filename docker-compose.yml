name: sit737-capstone # This groups all containers under this project name

services:
  # === 1. DATABASE ===
  postgres:
    image: postgres:16-alpine
    container_name: capstone_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-blueberry_admin}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-blueberry123}
      POSTGRES_DB: ${DB_NAME:-blueberry_hms}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - capstone_network
    healthcheck:
      test:
        - "CMD-SHELL"
        - "pg_isready -U blueberry_admin -d blueberry_hms"
      interval: 10s
      timeout: 5s
      retries: 5

  # === 2. BACKEND API ===
  api-core:
    build:
      context: .
      dockerfile: Dockerfile.api
    container_name: capstone_api
    restart: unless-stopped
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: ${DB_USER:-blueberry_admin}
      DB_PASSWORD: ${DB_PASSWORD:-blueberry123}
      DB_NAME: ${DB_NAME:-blueberry_hms}
      JWT_SECRET: ${JWT_SECRET:-blueberry_super_secret_key_capstone_2024}
    ports:
      - "4000:4000"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - capstone_network

  # === 3. FRONTEND ADMIN PANEL ===
  admin-panel:
    build:
      context: .
      dockerfile: Dockerfile.admin
    container_name: capstone_admin
    restart: unless-stopped
    ports:
      - "8080:80" 
    depends_on:
      - api-core
    networks:
      - capstone_network

networks:
  capstone_network:
    name: capstone_network
    driver: bridge

volumes:
  postgres_data: